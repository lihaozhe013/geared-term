# Geared Term requirements matrix

This matrix tracks the current implementation against the normative requirement groups in
[`SPEC.md`](../SPEC.md). A row is marked **implemented** only when code and automated verification
exist in this repository. UI completion, storage migration, and packaged end-to-end behavior remain
open until their dedicated fixtures and tests are added. See
[`docs/architecture.md`](architecture.md) for the implementation design behind the evidence columns.

| Requirement group             | Current status     | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Remaining gate                                                                         |
| ----------------------------- | ------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------- |
| APP application shell         | implemented        | `apps/desktop/src/renderer/src/App.tsx`, terminal tabs, connection-name tab labels with live profile tracking and active-tab window title (APP-027), native menu, persisted sidebar/UI state, modal focus containment with focus return, and secure window creation in `apps/desktop/src/main/index.ts`, verified by Playwright window/tab/dialog E2E                                                                                                                                                                                                                                                                                                                                                                   | keyboard-only audit and packaged verification                                          |
| AUX auxiliary windows         | implemented        | single-instance `SettingsWindowManager` with category navigation, `remote-editor-window.ts` with conflict-checked saves, `terminal-snapshot-window.ts` single-instance draft editor, and Windows/Linux `TrayController` background mode with second-launch re-show, covered by Playwright `background-mode.spec.ts`                                                                                                                                                                                                                                                                                                                                                                                                     | packaged tray/editor verification on each OS                                           |
| SES sessions and profiles     | partial            | versioned profile schema, profile list/save/delete IPC, grouped profile sidebar with persistent session and group drag ordering and validated reorder IPC, `ProfileEditor.tsx` for local/WSL/SSH targets, one-shot SSH dialog, and saved SSH resolution                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | environment identity semantics                                                         |
| VLT vault and secrets         | implemented        | `apps/desktop/src/main/vault/vault.ts`, transactional password rotation with compensating rollback, safeStorage auto-unlock (ADR 0001), vault tests, renderer-free IPC, and Playwright E2E lifecycle (initialize/lock/unlock-wrong/rotate)                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | packaged OS keychain verification                                                      |
| TERM terminal transport       | implemented        | `apps/desktop/src/main/local-terminal.ts`, protocol schemas, xterm renderer, tab lifecycle, persisted settings, bounded context snapshots with assistant preview, font-table-probed ligatures (`ligatures.ts`), copy/paste/select-all/search bindings, snapshot scratch editor (TERM-023), Windows x64 Microsoft ConPTY runtime pinned by ADR 0004, and Playwright local-shell typing/tab, ligature, and webgl-renderer E2E                                                                                                                                                                                                                                                                 | accessibility and packaging stress tests                                               |
| LOC local shells              | implemented (core) | shell resolution and ConPTY/PTY lifecycle in `local-terminal.ts`, Windows x64 runtime staging and packaged cmd/PowerShell smoke (ADR 0004), plus executable/arguments/cwd profile fields                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | WSL packaged UI and shell-specific validation                                          |
| WSL sessions                  | partial            | UTF-8/UTF-16 discovery parser, validated discovery IPC, confirmed double-click launch tabs, and bounded WSL environment probe                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Windows-only E2E                                                                       |
| SSH sessions                  | implemented (core) | `apps/desktop/src/main/ssh/ssh-session.ts`, saved/temporary connection forms, vault-backed password/private-key/passphrase replacement, known-host tests, and host-key dialog                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | SSH integration fixtures and reconnect/error-flow coverage                             |
| SFTP                          | implemented        | dual-pane browser with full context menus (entry, blank-region, and multi-selection actions), Windows drive overview with guarded roots, stable-ID byte-progress transfers grouped per top-level item with cancellation, canonical listing, bidirectional `cd` sync (panel navigation injects `cd`; renderer reconstructs tab/history-edited lines from the screen echo; quiet `stty`/`pwd` probe re-sync and self-heal; pause/detach/re-sync), sanitized remote-file commands, size-capped remote editing with save-conflict detection (SFTP-014, APP-024), and end-to-end `ls`/`mkdir`/`rename`/`remove`/upload/download/cancel coverage against the in-repo controlled SSH/SFTP server (`controlled-server.test.ts`) | packaged SFTP verification                                                             |
| FILES local and remote files  | implemented        | `LocalFilePane` is shared by the Local Shell single-pane browser and SSH's dual-pane `SftpPanel`; Local Shell startup cwd IPC (`local:working-directory`) is stored per session, later navigation is independent, WSL is unavailable, and existing validated local operations provide selection, mutations, default-app open, path copy, and file-manager open/reveal. SSH retains canonical SFTP listing, bidirectional `cd` sync, transfers, remote commands, and detach/re-sync                                                                                                                                                                                                                                      | packaged verification and full Playwright local Files workflow                         |
| ENV environment context       | partial            | bounded local/WSL probe parser, versioned environment persistence, validated IPC, and context editor panel                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | SSH probes, richer AI context redaction, and conflict-aware refresh                    |
| AI assistant                  | implemented        | endpoint validation, vault-referenced connections, cancellable AI MessagePort, provider adapter, SSE parser, model discovery, Markdown conversation history, sanitized/highlighted rendering, snapshot context, environment attachment, and `AssistantPanel.tsx`                                                                                                                                                                                                                                                                                                                                                                                                                                                        | end-to-end provider coverage                                                           |
| CMD command actions           | partial            | bounded conservative parser, content revisions, main-side revision/run validation, destructive-risk classification, optional split command cards, and copy/insert/run gating with Playwright E2E for stale-revision, destructive-run, and gated insert                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | stale-session UX                                                                       |
| SET settings and localization | implemented        | grouped settings (General/Appearance/Terminal/Shortcuts/SFTP/AI/About), rebindable validated shortcuts with conflict reporting (`packages/keybindings`), theme engine with user JSON loading + invalid-file isolation, Catppuccin built-ins, ordered fallback fonts, configurable terminal padding with automatic full-screen fill (SET-004, ADR 0002), native menu, and language refresh                                                                                                                                                                                                                                                                                                                               | packaged verification and catalog completeness audit                                   |
| DATA persistence              | implemented        | SQLite profile store (embedded database, transactional writes, WAL + quick_check, pre-migration VACUUM backups, quarantine tests), one-shot JSON adoption, and persisted environment records                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | packaged native-module smoke                                                           |
| SEC Electron boundary         | implemented (core) | context isolation, sandbox, CSP, navigation/new-window blocking, validated IPC, Playwright assertions that the renderer stays sandboxed behind a frozen preload allowlist, and a built-bundle security audit (`pnpm --filter @geared-term/desktop audit:security`) with a clean `pnpm audit` baseline                                                                                                                                                                                                                                                                                                                                                                                                                   | recurring dependency audit and packaged security review                                |
| OBS diagnostics               | implemented (core) | redacted category logs with startup/size rotation; redaction handles JSON-serialized details with regression tests                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | crash reporting hooks and release-log verification                                     |
| UPD update delivery           | implemented (core) | `apps/desktop/src/main/update-manager.ts` SHA-compared nightly checks (startup + 24 h + manual from Settings → About), `update-release.ts` GitHub API parsing, electron-updater NSIS auto-download/install gated to packaged Windows installer builds, `beta.yml` generic feed published by `.github/workflows/nightly.yml`, and the validated `UpdateStatus` state machine plus a localized native prompt shown once per run and queued while the main window is hidden                                                                                                                                                                                                                                                | updater fixture coverage and release-channel verification                              |
| REL reliability               | partial            | cleanup paths, bounded queues/timeouts, unit tests, and SSH-layer stress coverage: sustained multi-megabyte output, rapid resize storms, concurrent sessions, abrupt network loss, SFTP transfer cancellation, renderer protocol violations, and host-key rejection                                                                                                                                                                                                                                                                                                                                                                                                                                                     | suspend/resume, WebGL fallback, renderer crash hooks, and packaged native-module tests |
| A11Y accessibility            | not started        | —                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | keyboard/focus audit, screen-reader semantics, scaling verification                    |

## Verification commands

The current checkpoint is reproducible with:

```text
pnpm typecheck
pnpm test
pnpm test:e2e
pnpm build
pnpm package
```

`pnpm test:e2e` builds the desktop bundle first, then drives the packaged output layout with
Playwright-managed Electron sessions using an isolated per-test user-data directory.
